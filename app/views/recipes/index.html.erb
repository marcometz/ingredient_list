<p id="notice"><%= notice %></p>

<h1>Recipes</h1>

<table>
  <thead>
    <tr>
      <th>Meal</th>
      <th>Ingredient</th>
      <th>Amount adult</th>
      <th>Amount teen</th>
      <th>Amount child</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @recipes.each do |recipe| %>
      <tr>
        <td><%= recipe.meal_id %></td>
        <td><%= recipe.ingredient_id %></td>
        <td><%= recipe.amount_adult %></td>
        <td><%= recipe.amount_teen %></td>
        <td><%= recipe.amount_child %></td>
        <td><%= link_to 'Show', recipe %></td>
        <td><%= link_to 'Edit', edit_recipe_path(recipe) %></td>
        <td><%= link_to 'Destroy', recipe, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Recipe', new_recipe_path %>
