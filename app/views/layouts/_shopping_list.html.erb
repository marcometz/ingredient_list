<div class="col-lg-4">
          <div class="card">
            <div class="card-header">
              Zutaten
            </div>
            <div class="card-body">
              <p class="card-text">
                Liste aller benötigten Zutaten für
                <ul>
                  <li><%= Setting.adults %> Erwachsene</li>
                  <li><%= Setting.teens %> Jugendliche</li>
                  <li><%= Setting.children %> Kinder</li>
                </ul>
              </p>
                <table class="table table-sm table-striped">
                  <thead>
                    <tr>
                      <th>Name</th>
                      <th>Menge</th>
                      <th colspan="2"></th>
                    </tr>
                  </thead>

                  <tbody>
                    <% @ingredients.each do |ingredient| %>
                      <% next if ingredient.total_count.to_f == 0.0 %>
                      <tr>
                        <td><%= ingredient.title %></td>
                        <td><%= ingredient.total_count %> <%= ingredient.unit %></td>
                        <td><%= link_to edit_ingredient_path(ingredient) do %><i class="far fa-edit"></i><% end %></td>
                      </tr>
                    <% end %>
                  </tbody>
                </table>

                <br>

                <%= link_to 'Neue Zutat', new_ingredient_path, class: "btn btn-primary" %>
            </div>
          </div>
        </div>
